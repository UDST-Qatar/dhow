---
// Sits between two <Section> elements as a sibling so the symmetric
// padding from each side forms equal whitespace above and below the rule.
---
<div class="mx-auto max-w-[var(--container-wide)] px-6 lg:px-10">
  <div class="border-t border-[color:var(--color-rule)]"></div>
</div>
